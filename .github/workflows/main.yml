on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test_job:
    runs-on: ubuntu-latest
    env:
      TEST: ${{ secrets.TEST_SECRET }}        
    steps:
      - name: Test Job
        shell: bash
        run: |
          echo "hello world"
